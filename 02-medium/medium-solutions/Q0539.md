## Q0539: [Minimum Time Difference](https://leetcode.com/problems/minimum-time-difference/)

```
Difficulty Level: MEDIUM
```

```
Description:

Given a list of 24-hour clock time points in "HH:MM" format, return the minimum minutes difference between any
two time-points in the list.
```

#### Python3:

```
class Solution:
    def findMinDifference(self, timePoints: List[str]) -> int:

        def time_to_mins(t):
            h, m = t.split(":")
            mins = int(h) * 60 + int(m)
            return mins

        mins = [time_to_mins(t) for t in timePoints]
        mins.sort()

        min_diff, n = 1440, len(mins)

        for i in range(n-1):
            curr_diff = mins[i+1] - mins[i]
            if curr_diff == 0: return 0
            min_diff = min(min_diff, curr_diff)

        min_diff = min(min_diff, 1440 + mins[0] - mins[-1])

        return min_diff
```

#### Java:

```
class Solution {
    public int findMinDifference(List<String> timePoints) {
        int n = timePoints.size();
        List<Integer> mins = new ArrayList<>();

        for (int i = 0; i < n; i++) {
            mins.add(timeToMins(timePoints.get(i)));
        }
        
        Collections.sort(mins);

        int min_diff = 1440;

        for (int i = 0; i < n-1; i++) {
            int curr_diff = mins.get(i+1) - mins.get(i);
            if (curr_diff == 0) return 0;
            min_diff = Math.min(min_diff, curr_diff);
        }

        min_diff = Math.min(min_diff, 1440 + mins.get(0) - mins.get(n-1));

        return min_diff;
    }

    public int timeToMins(String t) {
        String h = t.substring(0, 2);
        String m = t.substring(3, 5);
        int mins = Integer.parseInt(h) * 60 + Integer.parseInt(m);
        return mins;
    }
}
```

#### JavaScript:

```
var findMinDifference = function(timePoints) {
    let n = timePoints.length;
    let mins = new Array(n);

    for (let i = 0; i < n; i++) {
        let t = timePoints[i];
        let h = t.substring(0, 2);
        let m = t.substring(3, 5);
        let min = Number(h) * 60 + Number(m);
        mins[i] = min;
    }

    mins.sort((a, b) => a - b);

    let min_diff = 1440;

    for (let i = 0; i < n-1; i++) {
        let curr_diff = mins[i+1] - mins[i];
        if (curr_diff == 0) return 0;
        min_diff = Math.min(min_diff, curr_diff);
    }

    min_diff = Math.min(min_diff, 1440 + mins[0] - mins[n-1]);

    return min_diff;
};
```

#### C++:

```
class Solution {
public:
    int findMinDifference(vector<string>& timePoints) {
        int n = timePoints.size();
        vector<int> mins;

        for (int i = 0; i < n; i++) {
            mins.push_back(timeToMins(timePoints[i]));
        }
        
       sort(mins.begin(), mins.end());

        int min_diff = 1440;

        for (int i = 0; i < n-1; i++) {
            int curr_diff = mins[i+1] - mins[i];
            if (curr_diff == 0) return 0;
            min_diff = min(min_diff, curr_diff);
        }

        min_diff = min(min_diff, 1440 + mins[0] - mins[n-1]);

        return min_diff;
    }

    int timeToMins(string& t) {
        string h = t.substr(0, 2);
        string m = t.substr(3, 5);
        int mins = stoi(h) * 60 + stoi(m);
        return mins;
    }
};
```

#### Scala:

```
object Solution {
    def findMinDifference(timePoints: List[String]): Int = {
        var n = timePoints.length
        var mins = new Array[Int](n)

        for (i <- 0 until n) {
            var Array(h, m) = timePoints(i).split(":")
            var min = h.toInt * 60 + m.toInt
            mins(i) = min
        }

        var sorted = mins.sorted

        var min_diff = 1440

        for (i <- 0 until n-1) {
            var curr_diff = sorted(i+1) - sorted(i)
            min_diff = Math.min(min_diff, curr_diff)
        }

        min_diff = Math.min(min_diff, 1440 + sorted(0) - sorted(n-1))

        min_diff
    }
}
```

#### Go:

```
func findMinDifference(timePoints []string) int {
    n := len(timePoints)
    mins := make([]int, n)

    for i := 0; i < n; i++ {
        t := strings.Split(timePoints[i], ":")
        h, _ := strconv.Atoi(t[0])
        m, _ := strconv.Atoi(t[1])
        min := h * 60 + m
        mins[i] = min
    }

    sort.Ints(mins)

    min_diff := 1440

    for i := 0; i < n-1; i++ {
        curr_diff := mins[i+1] - mins[i]
        if curr_diff == 0 {return 0}
        min_diff = min(min_diff, curr_diff)
    }

    min_diff = min(min_diff, 1440 + mins[0] - mins[n-1])

    return min_diff
}
```

#### C#:

```
public class Solution {
    public int FindMinDifference(IList<string> timePoints) {
        int n = timePoints.Count;
        List<int> mins = new List<int>();

        for (int i = 0; i < n; i++) {
            mins.Add(TimeToMins(timePoints[i]));
        }
        
        mins.Sort();

        int min_diff = 1440;

        for (int i = 0; i < n-1; i++) {
            int curr_diff = mins[i+1] - mins[i];
            if (curr_diff == 0) return 0;
            min_diff = Math.Min(min_diff, curr_diff);
        }

        min_diff = Math.Min(min_diff, 1440 + mins[0] - mins[n-1]);

        return min_diff;
    }

    public int TimeToMins(string t) {
        string h = t.Substring(0, 2);
        string m = t.Substring(3, 2);
        int mins = int.Parse(h) * 60 + int.Parse(m);
        return mins;
    }
}
```

#### Swift:

```

```